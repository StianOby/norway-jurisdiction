{
  "name": "norway-jurisdiction",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive 3D teaching model of Norway's maritime, seabed and airspace jurisdiction",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data:fetch": "python scripts/fetch_boundaries.py",
    "data:build": "python scripts/polygonise.py && python scripts/build_zones.py --check"
  },
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
